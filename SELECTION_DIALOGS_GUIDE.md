# 选择对话框使用指南

## 概述

根据需求分析文档3.2.10-3.2.13章节，已实现四个选择对话框模块：
- **模板选择对话框** (3.2.10)
- **身份证复印件选择对话框** (3.2.11)
- **统一社会信用代码证书选择对话框** (3.2.12)
- **合同文件选择对话框** (3.2.13)

## 功能特点

### 1. 模板选择对话框 (`TemplateSelectionDialog`)

**用途**: 创建项目文件夹时选择并复制模板

**功能**:
- 分层显示模板列表（最近使用的10条 + 所有模板）
- 支持模板搜索（根据名称模糊查询）
- 显示模板详细信息（编程语言、IDE、数据库类型等）
- 双击或点击"选择并复制"按钮复制模板到项目文件夹
- 自动命名：`项目ID.项目名称`
- 更新模板的 `recurrent_copy_time` 字段

**调用位置**: 任务视图 → "创建项目文件夹"按钮

### 2. 身份证复印件选择对话框 (`IDCardFileDialog`)

**用途**: 选择身份证复印件并复制到项目文件夹

**功能**:
- 分层显示文件列表（最近使用的10条 + 所有文件）
- 支持文件搜索（根据文件名模糊查询）
- 显示文件详细信息（姓名、性别、籍贯、身份证号等）
- 双击或点击"选择并复制"按钮复制文件到项目文件夹
- 自动处理文件名冲突（添加_1, _2后缀）
- 更新文件的 `recurrent_copy_time` 字段

**调用位置**: 任务视图 → "身份证复印件"按钮

### 3. 统一社会信用代码证书选择对话框 (`USCCCFileDialog`)

**用途**: 选择统一社会信用代码证书并复制到项目文件夹

**功能**:
- 分层显示文件列表（最近使用的10条 + 所有文件）
- 支持文件搜索（根据文件名模糊查询）
- 显示文件详细信息（事业单位名称、证件有效期、法人代表等）
- 双击或点击"选择并复制"按钮复制文件到项目文件夹
- 自动处理文件名冲突（添加_1, _2后缀）
- 更新文件的 `recurrent_copy_time` 字段

**调用位置**: 任务视图 → "统一社会信用代码证书"按钮

### 4. 合同文件选择对话框 (`ContractFileDialog`)

**用途**: 选择合同文件并复制到项目文件夹

**功能**:
- 分层显示文件列表（最近使用的10条 + 所有文件）
- 支持文件搜索（根据文件名模糊查询）
- 显示文件详细信息（合同名称、合同参与人类型等）
- 双击或点击"选择并复制"按钮复制文件到项目文件夹
- 自动处理文件名冲突（添加_1, _2后缀）
- 更新文件的 `recurrent_copy_time` 字段

**调用位置**: 任务视图 → "合同"按钮

## 界面设计

所有对话框采用统一的左右分栏布局：

### 左侧面板 (Left Frame, 宽度=300)
- **Treeview控件**: 分层显示文件/模板列表
  - 第一层：最近使用的记录（最多10条，按 `recurrent_copy_time` 倒序）
  - 第二层：所有记录
- **搜索框架**: 标签 + 文本框 + 检索按钮

### 右侧面板 (Right Frame)
- **标题**: 显示当前内容类型
- **详细信息区**: 显示选中项的详细信息（只读）
- **底部按钮**: "选择并复制" + "取消"

## 使用流程

### 1. 模板选择流程

```
1. 用户在任务视图中选择一个项目
2. 点击"创建项目文件夹"按钮
3. 系统打开模板选择对话框
4. 用户浏览模板列表或使用搜索功能
5. 单击模板查看详情，双击或点击"选择并复制"
6. 系统复制模板到项目文件夹并重命名
7. 更新模板的最近使用时间
8. 关闭对话框，刷新项目详情
```

### 2. 材料文件选择流程

```
1. 用户在任务视图中选择一个项目
2. 点击相应的材料按钮（身份证/证书/合同）
3. 系统检查项目文件夹是否存在
   - 不存在: 提示是否先创建项目文件夹
   - 存在: 继续
4. 系统打开相应的文件选择对话框
5. 用户浏览文件列表或使用搜索功能
6. 单击文件查看详情，双击或点击"选择并复制"
7. 系统复制文件到项目文件夹
   - 如有同名文件，自动添加后缀(_1, _2...)
8. 更新文件的最近使用时间
9. 关闭对话框，刷新项目文件列表
```

## 数据库字段

所有相关表都包含 `recurrent_copy_time` 字段：

- `template_folders.recurrent_copy_time`: TEXT
- `id_card_files.recurrent_copy_time`: TEXT
- `usccc_files.recurrent_copy_time`: TEXT
- `contract_files.recurrent_copy_time`: TEXT

此字段记录最近一次使用（复制）的时间，用于排序和显示"最近使用"列表。

## 文件结构

```
desktop_app/
├── selection_dialogs.py          # 四个选择对话框的实现
├── task_view_new.py              # 任务视图（已集成对话框）
├── local_models.py                # 本地数据库模型
└── migrate_local_db.py           # 数据库迁移脚本

migrate/
└── quick_migrate_local_db.py     # 快速迁移脚本
```

## 技术实现

### 核心类

1. **TemplateSelectionDialog**: 模板选择对话框
2. **IDCardFileDialog**: 身份证复印件选择对话框
3. **USCCCFileDialog**: 统一社会信用代码证书选择对话框
4. **ContractFileDialog**: 合同文件选择对话框

### 关键方法

- `load_files()` / `load_templates()`: 加载并分层显示数据
- `on_select()`: 处理选择事件，显示详情
- `on_double_click()`: 处理双击事件，直接复制
- `search_file()` / `search_template()`: 搜索功能
- `confirm_selection()`: 确认选择并执行复制操作
- `show()`: 显示对话框并等待用户操作

### 文件复制逻辑

```python
# 处理文件名冲突
if os.path.exists(target_path):
    base, ext = os.path.splitext(file_name)
    counter = 1
    while os.path.exists(target_path):
        new_name = f"{base}_{counter}{ext}"
        target_path = os.path.join(target_folder, new_name)
        counter += 1

# 复制文件
shutil.copy2(source_file, target_path)

# 更新最近使用时间
model.update_file(
    file_id,
    recurrent_copy_time=datetime.now().isoformat()
)
```

## 注意事项

1. **项目文件夹检查**: 复制材料文件前会检查项目文件夹是否存在，如不存在会提示用户先创建
2. **文件名冲突处理**: 自动添加数字后缀避免覆盖
3. **数据库同步**: 每次复制后会更新数据库中的 `recurrent_copy_time` 字段
4. **错误处理**: 所有操作都包含异常捕获和用户友好的错误提示
5. **窗口居中**: 对话框自动在屏幕中央显示
6. **模态对话框**: 使用 `transient` 和 `grab_set` 确保模态行为

## 测试建议

1. **测试模板复制**:
   - 选择项目 → 创建项目文件夹 → 选择模板 → 验证文件夹创建和命名
   - 检查 `recurrent_copy_time` 是否更新

2. **测试材料文件复制**:
   - 选择项目 → 点击材料按钮 → 选择文件 → 验证文件复制
   - 重复选择同一文件 → 验证冲突处理

3. **测试搜索功能**:
   - 输入关键词 → 点击检索 → 验证定位和高亮

4. **测试最近使用排序**:
   - 多次使用不同的文件/模板 → 验证"最近使用"列表的排序

## 扩展建议

1. 支持批量选择和复制
2. 支持拖拽操作
3. 添加文件预览功能
4. 支持文件重命名
5. 添加使用频率统计

