# 流水号获取功能实现说明

## 一、功能概述

本功能实现了从国家版权保护中心网站（https://register.ccopyright.com.cn）自动提取软件著作权流水号的功能。用户在桌面应用中点击"获取流水号"按钮后，系统会启动浏览器，导航至版权中心的软件登记页面，在用户完成登录后，自动从页面中提取匹配的项目流水号，并回填到桌面应用中。

## 二、技术架构

### 2.1 核心模块

- **desktop_app/serial_fetcher_simple.py**: 流水号提取核心模块
  - 使用 Playwright 控制浏览器
  - 执行 JavaScript 提取页面数据
  - 匹配项目名称和申请人名称
  - 返回流水号

- **desktop_app/task_view_new.py**: 任务视图集成
  - `fetch_serial_number()` 方法调用流水号提取功能
  - 回填流水号到界面控件
  - 同步更新到服务器数据库

- **desktop_app/server_client.py**: 服务器通信
  - `update_project_serial()` 方法更新流水号到服务器
  - 基于 `update_project_fields()` 通用更新接口

### 2.2 依赖技术

- **Playwright**: 浏览器自动化工具
- **Tkinter**: GUI 对话框
- **正则表达式**: 文本匹配和提取
- **JavaScript**: 页面数据提取

## 三、工作流程

### 3.1 用户操作流程

1. 用户在桌面应用的任务视图中选择一个项目
2. 点击"获取流水号"按钮
3. 系统启动 Chromium 浏览器（已优化为全屏显示）
4. 浏览器自动导航到版权中心的软件登记页面
5. **系统检测登录状态**：
   - 如果已登录：显示"检测到您已登录"提示
   - 如果未登录：显示"需要登录版权中心"提示
6. 用户根据提示完成登录（仅首次需要）
7. 用户点击对话框"是"按钮继续
8. **系统自动在搜索框中输入软件名称**
9. **系统自动点击搜索按钮**
10. **系统从搜索结果中提取匹配的流水号**
11. **如果有多个匹配结果，弹出选择对话框供用户选择**
12. 流水号回填到桌面应用的相关控件
13. 系统自动同步流水号到服务器数据库（如可用）
14. 浏览器自动关闭
15. **登录状态自动保存，下次使用时无需重新登录**

### 3.2 技术实现流程

```
用户点击按钮
    ↓
读取项目信息（项目名称、申请人名称）
    ↓
启动 Playwright 浏览器
    ↓
设置浏览器视口为全屏尺寸
    ↓
导航到版权中心页面
    ↓
优化页面显示（设置 CSS 样式）
    ↓
检查登录状态（检测页面元素）
    ↓
根据登录状态显示不同对话框
    ↓
用户确认（已登录直接继续，未登录需先登录）
    ↓
查找搜索输入框（多种选择器备用）
    ↓
在搜索框中输入软件名称
    ↓
查找并点击搜索按钮（多种选择器备用）
    ↓
等待搜索结果加载
    ↓
执行 JavaScript 提取搜索结果列表
    ↓
遍历搜索结果，匹配项目名称
    ↓
（可选）匹配申请人名称
    ↓
处理匹配结果：
    - 0个结果：显示未找到提示
    - 1个结果：直接返回流水号
    - 多个结果：弹出选择对话框
    ↓
回填到界面控件
    ↓
更新服务器数据库
    ↓
关闭浏览器
```

## 四、关键技术实现

### 4.1 新搜索方案概述

**版本更新说明**：原版本只能扫描当前页面显示的10条记录，存在局限性。新版本通过搜索功能获取所有匹配的记录，大大提高了成功率。

**新方案优势**：
1. **全面搜索**：通过搜索功能获取所有同名项目，不受当前页面显示数量限制
2. **智能匹配**：支持项目名称和申请人名称的双重匹配
3. **用户选择**：当有多个匹配结果时，提供友好的选择界面
4. **降级处理**：搜索失败时自动回退到原方案
5. **🔐 登录状态持久化**：自动保存登录信息，下次使用时无需重新登录

### 4.1.1 登录状态持久化

**问题解决**：原版本每次启动浏览器都需要重新输入账号密码，用户体验不佳。

**解决方案**：
1. **用户数据目录**：使用 `--user-data-dir` 参数指定持久化目录
2. **登录状态检测**：自动检测页面是否已登录
3. **智能提示**：根据登录状态显示不同的用户提示
4. **数据安全**：用户数据保存在项目目录下，便于管理

**技术实现**：
```python
# 获取用户数据目录
user_data_dir = os.path.join(project_dir, 'browser_data', 'copyright_center')
os.makedirs(user_data_dir, exist_ok=True)

# 使用 launch_persistent_context 保存用户数据
context = p.chromium.launch_persistent_context(
    user_data_dir=user_data_dir,  # 关键：保存用户数据
    headless=False,
    args=[
        '--start-maximized',
        '--no-sandbox',
        '--disable-web-security',
    ],
    viewport={'width': screen_width, 'height': screen_height},
    device_scale_factor=1
)
```

#### 4.1.2 登录状态检测

系统会自动检测页面是否已登录，避免重复登录：

```python
def _check_login_status(page):
    """检查是否已经登录"""
    try:
        # 检查页面是否包含登录相关元素
        login_elements = page.query_selector_all('input[type="text"], input[type="password"], button:has-text("登录")')
        
        if len(login_elements) > 0:
            # 检查是否有用户名输入框
            username_input = page.query_selector('input[type="text"]')
            if username_input:
                # 检查输入框是否有值
                value = username_input.input_value()
                if value and value.strip():
                    return True  # 已登录
                else:
                    return False  # 需要登录
        
        # 检查是否有用户中心相关元素
        user_center_elements = page.query_selector_all('text="用户中心", text="我的登记", text="软件登记"')
        if len(user_center_elements) > 0:
            return True  # 已登录
            
        return False  # 需要登录
        
    except Exception as e:
        return False  # 检测失败，假设需要登录
```

#### 4.1.3 智能用户提示

根据登录状态显示不同的对话框：

- **已登录**：显示"检测到您已登录"，直接开始搜索
- **未登录**：显示"需要登录版权中心"，提示用户登录并说明状态会保存

### 4.2 搜索功能实现

#### 4.2.1 搜索输入框定位

使用多种选择器策略确保找到搜索输入框：

```python
# 主要选择器（用户提供的精确路径）
search_input_selector = "#app > div.account_container > div > div.account_right > div.hd-tabs > div.content > div:nth-child(1) > div > div.hd-input > div.center > div > input[type=text]"

# 备用选择器
alternative_selectors = [
    'input[type="text"]',
    '.hd-input input[type="text"]',
    '.center input[type="text"]',
    'input[placeholder*="软件"]',
    'input[placeholder*="名称"]'
]
```

#### 4.2.2 搜索按钮定位

使用多种策略查找搜索按钮：

```python
search_button_selectors = [
    'button:has-text("查询")',
    'button:has-text("搜索")',
    'button:has-text("检索")',
    'input[type="submit"]',
    'button[type="submit"]',
    '.hd-btn',
    'button'
]
```

#### 4.2.3 搜索执行流程

```python
# 1. 填写搜索内容
page.fill(search_input_selector, project_name)

# 2. 点击搜索按钮
for selector in search_button_selectors:
    try:
        page.click(selector)
        break
    except:
        continue

# 3. 等待搜索结果
page.wait_for_load_state('networkidle', timeout=30000)
```

### 4.3 网页结构分析

通过 MCP Playwright 工具分析版权中心网站，发现项目列表的 DOM 结构如下：

```html
<li>
  流水号：2025R11L2811316 2025-10-03
  智慧病历质控管理系统
  我代理邵春晓申请
  待受理
  查看详情
  1316.pdf
  删除申请
</li>
```

每个项目是一个 `<li>` 元素，包含：
- 第1行：流水号和日期
- 第2行：软件名称
- 第3行：申请人信息
- 第4行：状态

### 4.2 JavaScript 数据提取

使用 Playwright 的 `page.evaluate()` 方法执行 JavaScript，提取所有项目信息：

```javascript
const allLis = document.querySelectorAll('li');
const projectLis = [];

allLis.forEach((li) => {
    const text = li.innerText || li.textContent || '';
    if (text.includes('流水号：')) {
        const lines = text.split('\\n').map(l => l.trim()).filter(l => l);
        
        // 提取流水号
        const serialMatch = text.match(/流水号：(\\d{4}R\\d+L\\d+)/);
        const serial = serialMatch ? serialMatch[1] : '';
        
        // 提取软件名称（第二行）
        let softwareName = '';
        let applicantInfo = '';
        
        lines.forEach((line, idx) => {
            if (line.includes('流水号')) {
                if (idx + 1 < lines.length) {
                    softwareName = lines[idx + 1];
                }
                if (idx + 2 < lines.length) {
                    applicantInfo = lines[idx + 2];
                }
            }
        });
        
        if (serial && softwareName) {
            projectLis.push({
                serial: serial,
                softwareName: softwareName,
                applicantInfo: applicantInfo
            });
        }
    }
});

return projectLis;
```

### 4.4 多结果选择对话框

当搜索到多个匹配项目时，系统会弹出选择对话框供用户选择：

#### 4.4.1 对话框特性

- **表格显示**：以表格形式展示所有匹配项目
- **详细信息**：显示序号、软件名称、申请人、流水号、状态
- **交互方式**：支持点击选择或双击确认
- **滚动支持**：支持大量结果的滚动查看

#### 4.4.2 对话框实现

```python
def _show_project_selection_dialog(projects: list, project_name: str, applicant_name: Optional[str] = None) -> Optional[dict]:
    # 创建 Tkinter 对话框
    dialog = tk.Toplevel()
    dialog.title("选择软件著作权项目")
    dialog.geometry("800x600")
    
    # 创建表格
    columns = ('序号', '软件名称', '申请人', '流水号', '状态')
    tree = ttk.Treeview(dialog, columns=columns, show='headings')
    
    # 插入数据
    for i, project in enumerate(projects, 1):
        tree.insert('', tk.END, values=(
            i,
            project['softwareName'],
            applicant_display,
            project['serial'],
            status
        ))
    
    # 等待用户选择
    dialog.wait_window()
    return selected_project
```

### 4.5 项目匹配逻辑

#### 4.3.1 项目名称匹配

使用模糊匹配策略，支持双向包含：

```python
if project_name in project['softwareName'] or project['softwareName'] in project_name:
    # 匹配成功
```

这样可以处理以下情况：
- 数据库中存储的是简称，网页上是全称
- 数据库中存储的是全称，网页上是简称
- 名称完全一致

#### 4.3.2 申请人名称匹配（可选）

如果提供了申请人名称，进行二次验证：

1. 从申请人信息中提取实际申请人名称
   - 格式："我代理XXX申请"
   - 使用正则表达式：`r'代理(.+?)申请'`

2. 匹配申请人名称
   - 模糊匹配：双向包含
   - 如果无法提取，则直接在申请人信息中查找

```python
if applicant_name:
    applicant_match = re.search(r'代理(.+?)申请', project['applicantInfo'])
    if applicant_match:
        actual_applicant = applicant_match.group(1).strip()
        if applicant_name in actual_applicant or actual_applicant in applicant_name:
            # 匹配成功
```

### 4.4 浏览器显示优化

为了提供更好的用户体验，实现了以下优化：

1. **动态屏幕尺寸检测**
   ```python
   import tkinter as tk
   root = tk.Tk()
   screen_width = root.winfo_screenwidth()
   screen_height = root.winfo_screenheight()
   root.destroy()
   ```

2. **浏览器启动优化**
   ```python
   browser = p.chromium.launch(
       headless=False,
       args=[
           '--start-maximized',
           '--window-size=1920,1080',
           '--no-sandbox',
       ]
   )
   ```

3. **视口设置**
   ```python
   context = browser.new_context(
       viewport={'width': screen_width, 'height': screen_height},
       device_scale_factor=1
   )
   page = context.new_page()
   page.set_viewport_size({'width': screen_width, 'height': screen_height})
   ```

4. **页面样式优化**
   ```javascript
   document.body.style.margin = '0';
   document.body.style.padding = '0';
   document.documentElement.style.margin = '0';
   document.documentElement.style.padding = '0';
   document.body.style.width = '100%';
   document.documentElement.style.width = '100%';
   document.body.style.overflow = 'auto';
   document.documentElement.style.overflow = 'auto';
   document.body.style.minHeight = '{screen_height}px';
   document.documentElement.style.minHeight = '{screen_height}px';
   ```

## 五、错误处理

### 5.1 常见错误及处理

| 错误类型 | 触发条件 | 处理方式 |
|---------|---------|---------|
| 项目名称为空 | 数据库中没有项目名称 | 显示警告对话框，提示用户 |
| 用户取消操作 | 用户点击对话框"否"按钮 | 关闭浏览器，返回 None |
| 未找到匹配项目 | 页面中没有匹配的项目 | 显示警告对话框，提示确认项目名称和页面 |
| JavaScript 执行失败 | 页面结构变化或网络问题 | 捕获异常，显示错误对话框 |
| Playwright 导入失败 | 未安装 Playwright | 显示错误对话框，提示安装命令 |
| 服务器同步失败 | 服务器不可用或网络问题 | 流水号仍回填到界面，提示服务器同步失败 |

### 5.2 调试支持

所有关键步骤都添加了 `print("[DEBUG] ...")` 调试输出，便于追踪问题：

```python
print(f"[DEBUG] 检测到屏幕尺寸: {screen_width}x{screen_height}")
print("[DEBUG] 浏览器启动成功")
print(f"[DEBUG] 设置页面视口为: {screen_width}x{screen_height}")
print("[DEBUG] 页面加载完成")
print("[DEBUG] 页面网络空闲")
print("[DEBUG] 页面已优化为全屏显示")
print(f"[DEBUG] 提取到 {len(projects_data)} 个项目")
print(f"[DEBUG] 检查项目: {project['softwareName']}, 流水号: {project['serial']}")
print(f"[DEBUG] 成功提取到流水号: {serial_number}")
```

## 六、测试说明

### 6.1 测试脚本

项目根目录下的 `test_serial_extraction.py` 提供了测试功能：

```bash
python test_serial_extraction.py
```

### 6.2 测试场景

脚本包含两个测试场景：

1. **场景1：只使用项目名称**
   - 项目名称："智慧病历质控管理系统"
   - 申请人：None
   - 预期结果：提取到流水号 "2025R11L2811316"

2. **场景2：使用项目名称和申请人名称**
   - 项目名称："智慧病历质控管理系统"
   - 申请人："邵春晓"
   - 预期结果：提取到流水号 "2025R11L2811316"

### 6.3 测试前提条件

1. 已安装 Playwright：`playwright install`
2. 能够访问版权中心网站
3. 拥有版权中心账号并知道密码
4. 版权中心账号下有测试项目

## 七、配置说明

### 7.1 目标 URL 配置

在 `desktop_app/serial_fetcher_simple.py` 中：

```python
TARGET_URL = 'https://register.ccopyright.com.cn/account.html?current=soft_register'
```

如需更改目标页面，修改此常量即可。

### 7.2 流水号正则表达式

流水号格式：`YYYYRNNLNNNNNNN`
- YYYY：年份
- R：固定字符
- NN：地区代码
- L：固定字符
- NNNNNNN：序列号

正则表达式：`r'流水号：(\\d{4}R\\d+L\\d+)'`

如流水号格式变化，修改此正则表达式。

## 八、扩展建议

### 8.1 支持多页面翻页

当前实现仅提取当前页面的项目列表。如果项目列表跨多页，可以扩展为：

1. 检测是否有"下一页"按钮
2. 循环点击"下一页"并提取数据
3. 直到找到匹配项目或遍历完所有页面

### 8.2 支持作品登记和专利登记

当前实现仅支持软件登记。可以扩展为：

1. 根据项目类型选择不同的目标 URL
2. 适配不同类型页面的 DOM 结构
3. 使用统一的接口函数

### 8.3 支持批量提取

当前实现每次提取一个项目的流水号。可以扩展为：

1. 接受项目列表作为参数
2. 在一次浏览器会话中提取多个流水号
3. 批量更新到数据库

### 8.4 支持无头浏览器模式

当前实现使用有界面浏览器（headless=False）。可以扩展为：

1. 实现自动登录功能
2. 使用无头浏览器模式（headless=True）
3. 提高执行速度和系统资源利用率

## 九、注意事项

### 9.1 网站结构变化

如果版权中心网站改版，DOM 结构可能发生变化，需要重新分析并更新 JavaScript 提取代码。

### 9.2 登录有效期

用户需要在对话框显示时完成登录。如果登录超时或会话失效，需要重新执行操作。

### 9.3 网络稳定性

功能依赖网络连接。建议在网络稳定的环境下使用。

### 9.4 浏览器兼容性

当前实现使用 Chromium 浏览器。如需支持其他浏览器（Firefox、WebKit），需要分别测试和适配。

## 十、故障排除

### 10.1 浏览器无法启动

**症状**: 点击"获取流水号"后无反应或报错

**解决方案**:
1. 确认已安装 Playwright：`playwright install`
2. 检查 Chromium 是否正确安装
3. 尝试手动运行测试脚本：`python test_serial_extraction.py`

### 10.2 无法找到项目

**症状**: 提示"未找到匹配的项目"

**解决方案**:
1. 确认项目名称是否正确
2. 确认项目是否已在版权中心登记
3. 确认是否在正确的页面（软件登记 vs 作品登记）
4. 检查浏览器中是否能看到该项目

### 10.3 流水号格式不正确

**症状**: 提取到的流水号格式不对或为空

**解决方案**:
1. 检查正则表达式是否匹配新的流水号格式
2. 查看调试输出，确认 JavaScript 是否正确提取
3. 手动检查网页上的流水号格式

### 10.4 服务器同步失败

**症状**: 流水号回填成功，但提示服务器同步失败

**解决方案**:
1. 检查网络连接
2. 确认服务器是否在线
3. 检查服务器日志
4. 稍后使用"保存修改"按钮手动同步

## 十一、版本历史

| 版本 | 日期 | 更新内容 | 作者 |
|------|------|---------|------|
| 1.0 | 2025-10-11 | 初始版本，实现基本流水号提取功能 | AI Assistant |
| 1.1 | 2025-10-11 | 优化浏览器显示，支持全屏和滚动条 | AI Assistant |
| 1.2 | 2025-10-11 | 基于网页结构分析，实现精确提取 | AI Assistant |
| 2.0 | 2025-10-11 | **重大更新**：实现基于搜索的流水号提取方案 | AI Assistant |
|      |      | - 通过搜索功能获取所有匹配记录，不受页面显示数量限制 |      |
|      |      | - 支持多种搜索输入框和按钮选择器 |      |
|      |      | - 实现多结果选择对话框 |      |
|      |      | - 添加降级处理机制 |      |
| 2.1 | 2025-10-11 | **用户体验优化**：实现登录状态持久化 | AI Assistant |
|      |      | - 添加用户数据目录，自动保存登录状态 |      |
|      |      | - 实现登录状态自动检测 |      |
|      |      | - 根据登录状态显示智能提示 |      |
|      |      | - 解决每次都需要重新登录的问题 |      |
| 2.1.1 | 2025-10-11 | **Bug修复**：修复Playwright用户数据目录参数错误 | AI Assistant |
|      |      | - 修复 launch() 方法中使用 --user-data-dir 参数的错误 |      |
|      |      | - 改用 launch_persistent_context() 方法 |      |
|      |      | - 确保登录状态持久化功能正常工作 |      |

## 十二、联系方式

如有问题或建议，请联系项目维护人员。

