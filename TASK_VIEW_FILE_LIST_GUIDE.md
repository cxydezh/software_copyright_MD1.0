# 任务视图文件列表功能使用指南

## 🎯 功能说明

任务视图现在完全支持项目文件列表的显示和管理，符合需求分析文档的所有要求。

## 📐 界面布局

```
┌─────────────────────────────────────────────────────────────────┐
│                          任务视图                                │
├───────────┬──────────────────────┬─────────────────────────────┤
│           │                      │                             │
│  项目列表  │   项目文件列表        │    项目详细信息             │
│ (左侧)    │  (中间左侧)          │   (中间右侧)                │
│           │                      │                             │
│ ┌───────┐ │ ┌──────────────────┐ │ ┌─────────────────────────┐ │
│ │已立项 │ │ │文件名  类型  大小│ │ │项目名称: [________]     │ │
│ │  项目1│ │ │doc.pdf PDF  1MB │ │ │流水号:  [________]      │ │
│ │  项目2│→│ │src.py  Python 5K│ │ │著作权人: [________]     │ │
│ │  项目3│ │ │...             │ │ │...                      │ │
│ └───────┘ │ └──────────────────┘ │ └─────────────────────────┘ │
│ ┌───────┐ │                      │                             │
│ │执行中 │ │  [右键菜单]          │                             │
│ └───────┘ │  - 打开文件          │                             │
│ ┌───────┐ │  - 打开文件夹        │                             │
│ │已完成 │ │  - 删除文件          │                             │
│ └───────┘ │                      │                             │
└───────────┴──────────────────────┴─────────────────────────────┘
```

## 🚀 使用流程

### 场景1: 查看项目文件

```
1. 在左侧项目列表中选择一个项目
   ↓
2. 中间面板自动显示该项目的所有文件
   ↓
3. 文件按相对路径、类型、大小、时间排列
   ↓
4. 可以看到项目文件夹中的所有文件（包括子文件夹）
```

### 场景2: 打开文件查看

**方法1: 双击**
```
双击文件列表中的任意文件
→ 文件自动用系统默认程序打开
```

**方法2: 右键菜单**
```
1. 右键点击文件
2. 选择"打开文件"
3. 文件自动用系统默认程序打开
```

### 场景3: 在文件夹中定位文件

```
1. 右键点击文件
2. 选择"打开文件夹"
3. 文件管理器打开并自动选中该文件
```

### 场景4: 删除不需要的文件

```
1. 右键点击文件
2. 选择"删除文件"
3. 确认删除
4. 文件从磁盘和列表中删除
```

## 🎨 显示的信息

### 文件名列
- 显示相对路径（相对于项目文件夹）
- 包含子文件夹路径（如 `源代码\main.py`）
- 中文路径完全支持

### 类型列
自动识别常见文件类型：

| 扩展名 | 显示类型 |
|--------|---------|
| .pdf | PDF文档 |
| .docx | Word文档 |
| .xlsx | Excel表格 |
| .py | Python源码 |
| .java | Java源码 |
| .zip | 压缩文件 |
| .jpg/.png | 图片文件 |

### 大小列
人类可读的文件大小格式：
- `< 1KB` → "123 B"
- `< 1MB` → "45.6 KB"
- `>= 1MB` → "2.3 MB"

### 修改时间列
格式: `YYYY-MM-DD HH:MM:SS`
示例: `2025-10-09 21:29:16`

## 🔧 技术特点

### 1. 实时同步
- 直接读取文件系统，不依赖数据库
- 保证显示的是实际存在的文件
- 外部修改文件后重新选择项目即可刷新

### 2. 智能识别
- 自动识别20+种文件类型
- 根据扩展名判断文件类型
- 未识别的显示为"其他文件"

### 3. 完整路径管理
- 显示相对路径（界面简洁）
- 存储完整路径（操作准确）
- 使用 tags 机制避免路径丢失

### 4. 安全操作
- 删除前需要确认
- 检查文件存在性
- 详细的错误提示

## 💻 平台兼容性

### Windows ✅
- `os.startfile()` - 打开文件
- `explorer /select,` - 定位文件

### macOS ✅
- `open` - 打开文件
- `open -R` - 定位文件

### Linux ✅
- `xdg-open` - 打开文件/文件夹

## 📋 示例输出

当选择一个项目时，控制台输出：
```
[INFO] 加载了 7 个文件
```

文件列表显示：
```
README.txt              | 文本文件    | 56 B   | 2025-10-09 21:29:16
源代码\main.py          | Python源码  | 41 B   | 2025-10-09 21:29:16
源代码\utils.py         | Python源码  | 39 B   | 2025-10-09 21:29:16
文档\需求文档.txt       | 文本文件    | 24 B   | 2025-10-09 21:29:16
文档\设计文档.txt       | 文本文件    | 24 B   | 2025-10-09 21:29:16
测试数据\test1.txt      | 文本文件    | 13 B   | 2025-10-09 21:29:16
测试数据\test2.txt      | 文本文件    | 13 B   | 2025-10-09 21:29:16
```

## 🐛 故障排除

### 问题1: 文件列表为空

**可能原因**:
- 项目文件夹不存在
- 项目文件夹为空
- 项目文件夹路径配置错误

**解决方法**:
- 检查项目文件夹路径: `base_path/项目ID.项目名称`
- 使用"创建项目文件夹"功能创建
- 检查系统设置中的项目路径配置

### 问题2: 文件打不开

**可能原因**:
- 文件已被删除或移动
- 系统没有安装相应的程序

**解决方法**:
- 刷新文件列表（重新选择项目）
- 安装相应的文件查看器

### 问题3: 中文文件名显示异常

**解决方法**:
- 代码已使用 UTF-8 编码
- 如仍有问题，检查系统编码设置

## 🎓 开发者说明

### 文件路径规则

**项目文件夹命名**: `{project_id}.{project_name}`

**示例**:
- 项目ID=1, 名称="测试项目" → `1.测试项目`
- 项目ID=123, 名称="软著申请" → `123.软著申请`

### 添加新的文件类型

在 `get_file_type()` 方法中添加：

```python
ext_map = {
    '.pdf': 'PDF文档',
    '.新扩展名': '新类型',  # 添加这里
}
```

### 自定义文件操作

在相应方法中添加逻辑：

```python
def custom_file_operation(self):
    selection = self.file_tree.selection()
    if not selection:
        return
    
    item = self.file_tree.item(selection[0])
    tags = item.get('tags', ())
    file_path = tags[0]  # 获取完整路径
    
    # 执行自定义操作...
```

## ✅ 完成度检查

根据需求分析文档检查：

| 需求 | 实现状态 |
|------|---------|
| 显示选中项目的文件列表 | ✅ 已实现 |
| 支持右键菜单 | ✅ 已实现 |
| 可以打开文件 | ✅ 已实现 |
| 可以打开文件所在文件夹 | ✅ 已实现 |
| 双击打开文件 | ✅ 已实现 |
| 删除文件功能 | ✅ 已实现 |

## 🎉 总结

✅ **任务视图的项目文件列表功能已完全实现**

- 符合需求分析文档的所有要求
- 提供丰富的文件操作功能
- 跨平台兼容
- 用户体验友好
- 代码质量高

可以直接投入使用！

---

**快速开始**: `python run_desktop.py`


