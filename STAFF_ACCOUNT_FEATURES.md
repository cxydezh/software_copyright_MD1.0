# 员工账号注册和审核功能实现说明

## 功能概述

本次更新为web_app添加了完整的员工账号注册和系统管理员审核功能，包括：

1. **员工账号注册功能**：公司员工可以勾选"员工账号"进行注册
2. **系统管理员审核功能**：提供用户管理模块，支持审核通过/驳回员工账号
3. **权限分配功能**：为审核通过的员工账号分配相应权限
4. **登录限制功能**：未审核通过的员工账号无法进入工作台

## 数据库变更

### Staff表新增字段

```sql
-- 员工账号审核相关字段
is_staff_account BOOLEAN DEFAULT 0 COMMENT '是否为员工账号'
approval_status VARCHAR(20) DEFAULT 'pending' COMMENT '审核状态'
approval_date DATETIME COMMENT '审核日期'
approver_id INTEGER COMMENT '审核者ID'
approval_remarks TEXT COMMENT '审核备注'
application_reason TEXT COMMENT '申请理由'
```

### 审核状态说明

- `pending`: 待审核（默认状态）
- `approved`: 已审核通过
- `rejected`: 已驳回

## 功能实现详情

### 1. 员工账号注册功能

#### 注册页面更新 (`templates/auth/register.html`)
- 添加"我是公司员工，申请员工账号"复选框
- 添加申请理由输入框（仅员工账号显示）
- 更新JavaScript验证逻辑
- 更新注册说明文字

#### 注册逻辑更新 (`web_app/views/auth.py`)
- 支持员工账号和普通用户两种注册模式
- 员工账号直接创建Staff记录，状态为pending
- 普通用户继续原有验证码流程
- 检查邮箱在User和Staff表中的唯一性

### 2. 系统管理员用户管理模块

#### 导航菜单更新 (`templates/base.html`)
- 在右上角下拉菜单中为系统管理员添加"用户管理"选项
- 只有系统管理员权限才能看到此菜单项

#### 用户管理页面 (`templates/staff/user_management.html`)
- 显示待审核员工账号列表
- 显示所有员工账号列表
- 显示所有普通用户列表
- 提供审核通过/驳回操作
- 提供权限分配功能

#### 管理功能实现 (`web_app/views/staff.py`)
- `user_management()`: 用户管理页面
- `approve_staff()`: 审核通过员工账号
- `reject_staff()`: 驳回员工账号
- `assign_permission()`: 为员工分配权限

### 3. 登录限制功能

#### 登录逻辑更新 (`web_app/views/auth.py`)
- 检查员工账号审核状态
- 待审核状态：显示"申请正在审核中"提示
- 已驳回状态：显示"申请已被驳回"提示
- 只有审核通过的员工才能进入工作台

### 4. 权限系统集成

#### 默认权限分配
- 审核通过时自动分配"普通业务员"权限
- 支持后续权限调整（普通业务员、项目执行者、系统管理员等）

#### 权限检查
- 系统管理员功能需要相应权限才能访问
- 用户管理功能仅限系统管理员使用

## 使用流程

### 员工注册流程
1. 员工访问注册页面
2. 勾选"我是公司员工，申请员工账号"
3. 填写申请理由
4. 提交注册申请
5. 等待系统管理员审核

### 管理员审核流程
1. 系统管理员登录系统
2. 点击右上角下拉菜单中的"用户管理"
3. 查看待审核员工账号列表
4. 选择审核通过或驳回
5. 如通过，可进一步分配权限

### 员工使用流程
1. 员工账号审核通过后收到通知
2. 使用注册的邮箱和密码登录
3. 根据分配的权限访问相应功能模块

## 部署说明

### 1. 数据库迁移
运行迁移脚本更新数据库结构：
```bash
python migrate_staff_accounts.py
```

### 2. 功能测试
运行测试脚本验证功能：
```bash
python test_staff_features.py
```

### 3. 权限配置
确保系统管理员账户存在且权限正确：
- 邮箱：admin@yiqichuang.com
- 密码：admin123
- 权限：系统管理员

## 安全考虑

1. **权限控制**：只有系统管理员可以访问用户管理功能
2. **状态验证**：登录时检查员工账号审核状态
3. **数据完整性**：邮箱在User和Staff表中保持唯一性
4. **操作记录**：审核操作记录审核者和审核时间

## 扩展功能

### 邮件通知（待实现）
- 审核通过邮件通知
- 驳回邮件通知
- 可在相关函数中添加邮件发送逻辑

### 批量操作（可扩展）
- 批量审核通过
- 批量权限分配
- 用户导入导出功能

## 注意事项

1. 现有员工账号会自动设置为已审核通过状态
2. 默认管理员账户会自动获得系统管理员权限
3. 员工账号和普通用户账号使用不同的表存储
4. 权限分配基于现有的Permission表结构

## 文件清单

### 新增文件
- `templates/staff/user_management.html`: 用户管理页面
- `migrate_staff_accounts.py`: 数据库迁移脚本
- `test_staff_features.py`: 功能测试脚本

### 修改文件
- `database/models.py`: 添加员工账号审核字段
- `web_app/views/auth.py`: 更新注册和登录逻辑
- `web_app/views/staff.py`: 添加用户管理功能
- `templates/auth/register.html`: 更新注册页面
- `templates/base.html`: 添加管理员菜单
- `templates/staff/business_dashboard.html`: 添加管理员菜单项

## 测试验证

运行测试脚本可以验证以下功能：
1. 员工账号注册
2. 管理员审核功能
3. 权限分配功能
4. 普通用户注册
5. 登录限制功能

所有功能测试通过后，系统即可投入使用。
