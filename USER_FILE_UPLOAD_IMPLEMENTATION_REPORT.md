# 用户申请项目文件上传功能实现报告

## 📋 功能概述

为用户申请项目页面 (`/user/apply_project`) 添加了文件上传和下载功能，用户可以在提交项目申请时上传附件，并在项目详情页面查看和下载已上传的文件。

## ✅ 实现内容

### 1. 后端API路由

**文件**: `web_app/views/user.py`

**新增路由**:
- `POST /user/apply_project/upload` - 用户申请项目时上传临时文件
- `GET /user/project/<project_id>/file/<file_id>/download` - 下载项目文件

**修改路由**:
- `POST /user/apply_project` - 修改为支持处理临时文件，提交申请后关联到项目

**功能特点**:
- 文件类型验证（支持PDF, DOC, DOCX, TXT, JPG, PNG, GIF, ZIP, RAR, XLS, XLSX）
- 文件安全存储（使用UUID生成唯一文件名）
- 文件分类管理（申请材料、技术文档、证书文件、其他文件）
- 权限验证（仅用户本人可以上传和下载）
- 错误处理和日志记录

### 2. 申请项目页面

**文件**: `templates/user/apply_project.html`

**新增功能**:
- 文件上传区域（支持多文件上传）
- 文件列表预览
- 文件删除功能
- 提交申请时自动上传文件并关联到项目

**修改内容**:
- 在表单中添加了文件上传区域
- 修改了表单提交逻辑，先上传文件再提交申请
- 添加了文件管理JavaScript函数

### 3. 项目详情页面

**文件**: `templates/user/project_detail.html`

**新增功能**:
- 文件列表显示（表格形式，包含文件名、类型、分类、大小、上传时间）
- 文件下载链接
- 文件大小格式化显示（B/KB/MB）

**修改内容**:
- 在项目消息卡片后添加了"项目文件"卡片
- 添加了文件列表显示

### 4. 数据库模型

**使用现有模型**: `ProjectFile`

**字段**:
- `project_id`: 项目ID
- `project_type`: 项目类型（'software'）
- `file_name`: 原始文件名
- `file_path`: 文件存储路径（相对路径）
- `file_type`: 文件扩展名
- `file_size`: 文件大小（字节）
- `uploader_id`: 上传者ID（用户ID）
- `file_category`: 文件分类
- `upload_time`: 上传时间

## 🔧 技术实现

### 文件存储结构

```
static/
  uploads/
    projects/
      {project_id}/
        {uuid}_{filename}  # 项目文件
    temp/
      {user_id}/
        {uuid}_{filename}  # 临时文件（申请时）
```

### 文件上传流程

**用户申请项目**:
1. 用户选择文件
2. 点击提交申请
3. JavaScript先上传文件到临时目录
4. 获取临时文件路径
5. 提交表单时传递临时文件路径
6. 后端创建项目后，将临时文件移动到项目目录
7. 创建文件数据库记录

### 文件下载流程

1. 用户点击"下载"按钮
2. 后端验证权限（只有项目申请人可以下载）
3. 从数据库获取文件信息
4. 构建文件完整路径
5. 使用Flask的`send_file`发送文件

## 📝 使用说明

### 申请项目页面

1. 访问：http://127.0.0.1:5000/user/apply_project
2. 填写项目信息
3. 在"附件上传（可选）"区域选择文件
4. 选择文件分类
5. 可以查看已选择的文件列表
6. 可以删除已选择的文件
7. 点击"提交申请"按钮
8. 系统会自动上传文件并关联到项目

### 项目详情页面

1. 访问：http://127.0.0.1:5000/user/project/{project_id}
2. 在"项目文件"卡片中查看已上传的文件
3. 点击文件列表中的"下载"按钮可以下载文件

## 🧪 测试

### 测试文件

- `test_user_file_upload.py`: 用户文件上传功能测试

### 测试内容

1. ✅ 创建测试用户
2. ✅ 创建测试项目
3. ✅ 测试文件记录创建
4. ✅ 测试文件查询
5. ✅ 清理测试数据

### 测试结果

所有测试通过！

## 📊 支持的文件类型

- 文档：PDF, DOC, DOCX, TXT
- 图片：JPG, JPEG, PNG, GIF
- 压缩：ZIP, RAR
- 表格：XLS, XLSX

## 🔍 权限控制

- **上传权限**: 仅登录用户可以在申请项目时上传文件
- **下载权限**: 只有项目申请人可以下载项目文件
- **查看权限**: 只有项目申请人可以看到文件列表

## ✨ 功能特点

1. **多文件上传**: 支持一次上传多个文件
2. **文件分类**: 支持文件分类管理
3. **文件预览**: 显示文件详细信息（名称、类型、大小、时间）
4. **安全存储**: 使用UUID生成唯一文件名，避免文件名冲突
5. **错误处理**: 完善的错误处理和日志记录
6. **用户友好**: 清晰的文件列表和操作界面

## 📁 修改文件列表

1. `web_app/views/user.py` - 添加文件上传和下载路由，修改申请项目路由
2. `templates/user/apply_project.html` - 添加文件上传功能
3. `templates/user/project_detail.html` - 添加文件列表显示
4. `test_user_file_upload.py` - 测试脚本

## 🎯 总结

用户申请项目文件上传和下载功能已成功实现，用户可以在申请项目时上传附件，并在项目详情页面查看和下载已上传的文件。所有功能已通过测试，可以正常使用。
