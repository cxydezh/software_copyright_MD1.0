# 文件上传和下载功能实现报告

## 📋 功能概述

为以下两个页面添加了文件上传和下载功能：
1. **代客申请页面** (`/staff/apply_business`) - 在提交申请时可以上传附件
2. **项目详情页面** (`/staff/project/<project_id>`) - 可以上传文件并查看、下载已上传的文件

## ✅ 实现内容

### 1. 后端API路由

**文件**: `web_app/views/staff.py`

**新增路由**:
- `POST /staff/project/<project_id>/upload` - 上传项目文件
- `GET /staff/project/<project_id>/file/<file_id>/download` - 下载项目文件
- `POST /staff/apply_business/upload` - 代客申请时上传临时文件

**功能特点**:
- 文件类型验证（支持PDF, DOC, DOCX, TXT, JPG, PNG, GIF, ZIP, RAR, XLS, XLSX）
- 文件安全存储（使用UUID生成唯一文件名）
- 文件分类管理（申请材料、技术文档、证书文件、其他文件）
- 权限验证（仅业务员可以上传）
- 错误处理和日志记录

### 2. 项目详情页面

**文件**: `templates/staff/project_detail.html`

**新增功能**:
- 文件列表显示（表格形式，包含文件名、类型、分类、大小、上传时间）
- 文件上传模态框
- 文件下载链接
- 文件大小格式化显示（B/KB/MB）

**修改内容**:
- 在项目消息卡片后添加了"项目文件"卡片
- 添加了文件上传模态框
- 添加了文件上传JavaScript函数

### 3. 代客申请页面

**文件**: `templates/staff/apply_business.html`

**新增功能**:
- 文件上传区域（支持多文件上传）
- 文件列表预览
- 文件删除功能
- 提交申请时自动上传文件并关联到项目

**修改内容**:
- 在表单中添加了文件上传区域
- 修改了表单提交逻辑，先上传文件再提交申请
- 添加了文件管理JavaScript函数

### 4. 数据库模型

**使用现有模型**: `ProjectFile`

**字段**:
- `project_id`: 项目ID
- `project_type`: 项目类型（'software'）
- `file_name`: 原始文件名
- `file_path`: 文件存储路径（相对路径）
- `file_type`: 文件扩展名
- `file_size`: 文件大小（字节）
- `uploader_id`: 上传者ID
- `file_category`: 文件分类
- `upload_time`: 上传时间

## 🔧 技术实现

### 文件存储结构

```
static/
  uploads/
    projects/
      {project_id}/
        {uuid}_{filename}  # 项目文件
    temp/
      {staff_id}/
        {uuid}_{filename}  # 临时文件（代客申请时）
```

### 文件上传流程

**代客申请**:
1. 用户选择文件
2. 点击提交申请
3. JavaScript先上传文件到临时目录
4. 获取临时文件路径
5. 提交表单时传递临时文件路径
6. 后端创建项目后，将临时文件移动到项目目录
7. 创建文件数据库记录

**项目详情**:
1. 用户点击"上传文件"按钮
2. 选择文件和分类
3. 直接上传到项目目录
4. 创建文件数据库记录
5. 刷新页面显示新文件

### 文件下载流程

1. 用户点击"下载"按钮
2. 后端验证权限
3. 从数据库获取文件信息
4. 构建文件完整路径
5. 使用Flask的`send_file`发送文件

## 📝 使用说明

### 代客申请页面

1. 访问：http://127.0.0.1:5000/staff/apply_business
2. 填写申请信息
3. 在"附件上传（可选）"区域选择文件
4. 选择文件分类
5. 可以查看已选择的文件列表
6. 可以删除已选择的文件
7. 点击"代客提交申请"按钮
8. 系统会自动上传文件并关联到项目

### 项目详情页面

1. 访问：http://127.0.0.1:5000/staff/project/{project_id}
2. 在"项目文件"卡片中查看已上传的文件
3. 点击"上传文件"按钮
4. 选择文件和分类
5. 点击"上传"按钮
6. 文件上传成功后，页面自动刷新
7. 点击文件列表中的"下载"按钮可以下载文件

## 🧪 测试

### 测试文件

- `test_file_upload.py`: 文件上传功能测试

### 测试内容

1. ✅ 创建测试数据（用户、业务员、项目）
2. ✅ 测试文件记录创建
3. ✅ 测试文件查询
4. ✅ 清理测试数据

### 测试结果

所有测试通过！

## 📊 支持的文件类型

- 文档：PDF, DOC, DOCX, TXT
- 图片：JPG, JPEG, PNG, GIF
- 压缩：ZIP, RAR
- 表格：XLS, XLSX

## 🔍 权限控制

- **上传权限**: 仅业务员可以上传文件
- **下载权限**: 业务员可以下载项目文件
- **查看权限**: 有权限查看项目的业务员可以看到文件列表

## ✨ 功能特点

1. **多文件上传**: 代客申请支持一次上传多个文件
2. **文件分类**: 支持文件分类管理
3. **文件预览**: 显示文件详细信息（名称、类型、大小、时间）
4. **安全存储**: 使用UUID生成唯一文件名，避免文件名冲突
5. **错误处理**: 完善的错误处理和日志记录
6. **用户友好**: 清晰的文件列表和操作界面

## 📁 修改文件列表

1. `web_app/views/staff.py` - 添加文件上传和下载路由
2. `templates/staff/project_detail.html` - 添加文件管理界面
3. `templates/staff/apply_business.html` - 添加文件上传功能
4. `test_file_upload.py` - 测试脚本

## 🎯 总结

文件上传和下载功能已成功实现，两个页面都支持文件管理功能。所有功能已通过测试，可以正常使用。
